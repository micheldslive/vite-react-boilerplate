import { {{pascalCase name}}Content } from './{{dashCase name}}.styles'

export const {{pascalCase name}} = () => (
  <div>
    <h1>{{pascalCase name}}</h1>
    <{{pascalCase name}}Content>{{pascalCase name}}</{{pascalCase name}}Content>
  </div>
)
